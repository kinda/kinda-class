{
  "name": "kinda-class",
  "version": "0.2.3",
  "description": "Simple and powerful dynamic object system",
  "author": "Manuel Vila <mvila@3base.com>",
  "main": "index.js",
  "repository": "git://github.com/kinda/kinda-class",
  "scripts": {
    "publish-patch": "(test -z \"$(git status --porcelain)\" && npm version patch && git push --follow-tags) || echo \"An error occured (unclean repository?)\"",
    "test": "mocha --ui tdd"
  },
  "license": "MIT",
  "devDependencies": {
    "chai": "^1.10.0",
    "mocha": "^2.0.1"
  }
}
